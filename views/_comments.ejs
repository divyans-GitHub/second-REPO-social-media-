
 <li>
    <h5 style="display: inline;"> <%= comment.user.name%> :</h5>
    <small> <%= comment.content %> </small>
    <% if(locals.user && ( locals.user.id == comment.user.id  || locals.user ) ){%> 
       
      <small>
        <a href="/comment/destroy/<%= comment.id %>">
          <i class="fas fa-times-circle"></i>
        </a>
      </small>
    <% } %> 
  </li>